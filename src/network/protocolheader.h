#ifndef PROTOCOLHEADER_H
#define PROTOCOLHEADER_H

#include <QObject>

class ProtocolHeader : public QObject
{
    Q_OBJECT
public:
    explicit ProtocolHeader(QObject *parent = 0);

signals:

public slots:

};

#endif // PROTOCOLHEADER_H
